# Frontend

当前前端是一个最小可运行的 `React + TypeScript + Vite + Tailwind CSS` 项目。

## 作用
- 提供一个极简状态页
- 请求后端的 `/api/health`
- 展示前后端和数据库连通性结果

## 启动
```bash
npm install
npm run dev
```

默认开发地址为 `http://localhost:5173`。
